Abstract:
    abstract class contain both abstract mathod(without body) and concreat mathod(with body). it allow code reuse and provides a common base for related classes

Interface:
    An interface is like a contract that defines a set of methods that a class must implement. 